# Wasm SIMD (fixed-width 128-bit) for the browser bundle: argon2's blake2
# core vectorizes, cutting a 64 MiB / t=16 key derivation from ~4 s to ~1 s.
# Supported by all evergreen browsers and iOS Safari >= 16.4.
[target.wasm32-unknown-unknown]
rustflags = ["-C", "target-feature=+simd128"]
